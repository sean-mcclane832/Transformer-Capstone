# Symlinked Obsidian vault — not part of this repo
notes/
notes

# Python cache
__pycache__/
*.pyc
*.pyo

# Trained checkpoint files (keep directory markers)
checkpoints/*.pt

# Pre-tokenized dataset tensors
data/processed/*.pt
data/*.pt

# Raw corpus text files (large; run data/build_corpus.py to regenerate)
data/raw/*.txt
